use stylist::{css, Style};

/// Tailwind `mix-blend-normal`; css `mix-blend-mode: normal;`
pub fn mix_blend_normal() -> Style {
    Style::new(css!("mix-blend-mode: normal;")).unwrap()
}

/// Tailwind `mix-blend-multiply`; css `mix-blend-mode: multiply;`
pub fn mix_blend_multiply() -> Style {
    Style::new(css!("mix-blend-mode: multiply;")).unwrap()
}

/// Tailwind `mix-blend-screen`; css `mix-blend-mode: screen;`
pub fn mix_blend_screen() -> Style {
    Style::new(css!("mix-blend-mode: screen;")).unwrap()
}

/// Tailwind `mix-blend-overlay`; css `mix-blend-mode: overlay;`
pub fn mix_blend_overlay() -> Style {
    Style::new(css!("mix-blend-mode: overlay;")).unwrap()
}

/// Tailwind `mix-blend-darken`; css `mix-blend-mode: darken;`
pub fn mix_blend_darken() -> Style {
    Style::new(css!("mix-blend-mode: darken;")).unwrap()
}

/// Tailwind `mix-blend-lighten`; css `mix-blend-mode: lighten;`
pub fn mix_blend_lighten() -> Style {
    Style::new(css!("mix-blend-mode: lighten;")).unwrap()
}

/// Tailwind `mix-blend-color-dodge`; css `mix-blend-mode: color-dodge;`
pub fn mix_blend_color_dodge() -> Style {
    Style::new(css!("mix-blend-mode: color-dodge;")).unwrap()
}

/// Tailwind `mix-blend-color-burn`; css `mix-blend-mode: color-burn;`
pub fn mix_blend_color_burn() -> Style {
    Style::new(css!("mix-blend-mode: color-burn;")).unwrap()
}

/// Tailwind `mix-blend-hard-light`; css `mix-blend-mode: hard-light;`
pub fn mix_blend_hard_light() -> Style {
    Style::new(css!("mix-blend-mode: hard-light;")).unwrap()
}

/// Tailwind `mix-blend-soft-light`; css `mix-blend-mode: soft-light;`
pub fn mix_blend_soft_light() -> Style {
    Style::new(css!("mix-blend-mode: soft-light;")).unwrap()
}

/// Tailwind `mix-blend-difference`; css `mix-blend-mode: difference;`
pub fn mix_blend_difference() -> Style {
    Style::new(css!("mix-blend-mode: difference;")).unwrap()
}

/// Tailwind `mix-blend-exclusion`; css `mix-blend-mode: exclusion;`
pub fn mix_blend_exclusion() -> Style {
    Style::new(css!("mix-blend-mode: exclusion;")).unwrap()
}

/// Tailwind `mix-blend-hue`; css `mix-blend-mode: hue;`
pub fn mix_blend_hue() -> Style {
    Style::new(css!("mix-blend-mode: hue;")).unwrap()
}

/// Tailwind `mix-blend-saturation`; css `mix-blend-mode: saturation;`
pub fn mix_blend_saturation() -> Style {
    Style::new(css!("mix-blend-mode: saturation;")).unwrap()
}

/// Tailwind `mix-blend-color`; css `mix-blend-mode: color;`
pub fn mix_blend_color() -> Style {
    Style::new(css!("mix-blend-mode: color;")).unwrap()
}

/// Tailwind `mix-blend-luminosity`; css `mix-blend-mode: luminosity;`
pub fn mix_blend_luminosity() -> Style {
    Style::new(css!("mix-blend-mode: luminosity;")).unwrap()
}

/// Tailwind `mix-blend-plus-darker`; css `mix-blend-mode: plus-darker;`
pub fn mix_blend_plus_darker() -> Style {
    Style::new(css!("mix-blend-mode: plus-darker;")).unwrap()
}

/// Tailwind `mix-blend-plus-lighter`; css `mix-blend-mode: plus-lighter;`
pub fn mix_blend_plus_lighter() -> Style {
    Style::new(css!("mix-blend-mode: plus-lighter;")).unwrap()
}

