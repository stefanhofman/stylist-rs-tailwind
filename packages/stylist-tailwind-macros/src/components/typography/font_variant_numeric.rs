use stylist::{css, Style};

/// Tailwind `normal-nums`; css `font-variant-numeric: normal;`
pub fn normal_nums() -> Style {
    Style::new(css!("font-variant-numeric: normal;")).unwrap()
}

/// Tailwind `ordinal`; css `font-variant-numeric: ordinal;`
pub fn ordinal() -> Style {
    Style::new(css!("font-variant-numeric: ordinal;")).unwrap()
}

/// Tailwind `slashed-zero`; css `font-variant-numeric: slashed-zero;`
pub fn slashed_zero() -> Style {
    Style::new(css!("font-variant-numeric: slashed-zero;")).unwrap()
}

/// Tailwind `lining-nums`; css `font-variant-numeric: lining-nums;`
pub fn lining_nums() -> Style {
    Style::new(css!("font-variant-numeric: lining-nums;")).unwrap()
}

/// Tailwind `oldstyle-nums`; css `font-variant-numeric: oldstyle-nums;`
pub fn oldstyle_nums() -> Style {
    Style::new(css!("font-variant-numeric: oldstyle-nums;")).unwrap()
}

/// Tailwind `proportional-nums`; css `font-variant-numeric: proportional-nums;`
pub fn proportional_nums() -> Style {
    Style::new(css!("font-variant-numeric: proportional-nums;")).unwrap()
}

/// Tailwind `tabular-nums`; css `font-variant-numeric: tabular-nums;`
pub fn tabular_nums() -> Style {
    Style::new(css!("font-variant-numeric: tabular-nums;")).unwrap()
}

/// Tailwind `diagonal-fractions`; css `font-variant-numeric: diagonal-fractions;`
pub fn diagonal_fractions() -> Style {
    Style::new(css!("font-variant-numeric: diagonal-fractions;")).unwrap()
}

/// Tailwind `stacked-fractions`; css `font-variant-numeric: stacked-fractions;`
pub fn stacked_fractions() -> Style {
    Style::new(css!("font-variant-numeric: stacked-fractions;")).unwrap()
}

