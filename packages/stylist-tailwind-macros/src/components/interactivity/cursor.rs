use stylist::{css, Style};

/// Tailwind `cursor-auto`; css `cursor: auto;`
pub fn cursor_auto() -> Style {
    Style::new(css!("cursor: auto;")).unwrap()
}

/// Tailwind `cursor-default`; css `cursor: default;`
pub fn cursor_default() -> Style {
    Style::new(css!("cursor: default;")).unwrap()
}

/// Tailwind `cursor-pointer`; css `cursor: pointer;`
pub fn cursor_pointer() -> Style {
    Style::new(css!("cursor: pointer;")).unwrap()
}

/// Tailwind `cursor-wait`; css `cursor: wait;`
pub fn cursor_wait() -> Style {
    Style::new(css!("cursor: wait;")).unwrap()
}

/// Tailwind `cursor-text`; css `cursor: text;`
pub fn cursor_text() -> Style {
    Style::new(css!("cursor: text;")).unwrap()
}

/// Tailwind `cursor-move`; css `cursor: move;`
pub fn cursor_move() -> Style {
    Style::new(css!("cursor: move;")).unwrap()
}

/// Tailwind `cursor-help`; css `cursor: help;`
pub fn cursor_help() -> Style {
    Style::new(css!("cursor: help;")).unwrap()
}

/// Tailwind `cursor-not-allowed`; css `cursor: not-allowed;`
pub fn cursor_not_allowed() -> Style {
    Style::new(css!("cursor: not-allowed;")).unwrap()
}

/// Tailwind `cursor-none`; css `cursor: none;`
pub fn cursor_none() -> Style {
    Style::new(css!("cursor: none;")).unwrap()
}

/// Tailwind `cursor-context-menu`; css `cursor: context-menu;`
pub fn cursor_context_menu() -> Style {
    Style::new(css!("cursor: context-menu;")).unwrap()
}

/// Tailwind `cursor-progress`; css `cursor: progress;`
pub fn cursor_progress() -> Style {
    Style::new(css!("cursor: progress;")).unwrap()
}

/// Tailwind `cursor-cell`; css `cursor: cell;`
pub fn cursor_cell() -> Style {
    Style::new(css!("cursor: cell;")).unwrap()
}

/// Tailwind `cursor-crosshair`; css `cursor: crosshair;`
pub fn cursor_crosshair() -> Style {
    Style::new(css!("cursor: crosshair;")).unwrap()
}

/// Tailwind `cursor-vertical-text`; css `cursor: vertical-text;`
pub fn cursor_vertical_text() -> Style {
    Style::new(css!("cursor: vertical-text;")).unwrap()
}

/// Tailwind `cursor-alias`; css `cursor: alias;`
pub fn cursor_alias() -> Style {
    Style::new(css!("cursor: alias;")).unwrap()
}

/// Tailwind `cursor-copy`; css `cursor: copy;`
pub fn cursor_copy() -> Style {
    Style::new(css!("cursor: copy;")).unwrap()
}

/// Tailwind `cursor-no-drop`; css `cursor: no-drop;`
pub fn cursor_no_drop() -> Style {
    Style::new(css!("cursor: no-drop;")).unwrap()
}

/// Tailwind `cursor-grab`; css `cursor: grab;`
pub fn cursor_grab() -> Style {
    Style::new(css!("cursor: grab;")).unwrap()
}

/// Tailwind `cursor-grabbing`; css `cursor: grabbing;`
pub fn cursor_grabbing() -> Style {
    Style::new(css!("cursor: grabbing;")).unwrap()
}

/// Tailwind `cursor-all-scroll`; css `cursor: all-scroll;`
pub fn cursor_all_scroll() -> Style {
    Style::new(css!("cursor: all-scroll;")).unwrap()
}

/// Tailwind `cursor-col-resize`; css `cursor: col-resize;`
pub fn cursor_col_resize() -> Style {
    Style::new(css!("cursor: col-resize;")).unwrap()
}

/// Tailwind `cursor-row-resize`; css `cursor: row-resize;`
pub fn cursor_row_resize() -> Style {
    Style::new(css!("cursor: row-resize;")).unwrap()
}

/// Tailwind `cursor-n-resize`; css `cursor: n-resize;`
pub fn cursor_n_resize() -> Style {
    Style::new(css!("cursor: n-resize;")).unwrap()
}

/// Tailwind `cursor-e-resize`; css `cursor: e-resize;`
pub fn cursor_e_resize() -> Style {
    Style::new(css!("cursor: e-resize;")).unwrap()
}

/// Tailwind `cursor-s-resize`; css `cursor: s-resize;`
pub fn cursor_s_resize() -> Style {
    Style::new(css!("cursor: s-resize;")).unwrap()
}

/// Tailwind `cursor-w-resize`; css `cursor: w-resize;`
pub fn cursor_w_resize() -> Style {
    Style::new(css!("cursor: w-resize;")).unwrap()
}

/// Tailwind `cursor-ne-resize`; css `cursor: ne-resize;`
pub fn cursor_ne_resize() -> Style {
    Style::new(css!("cursor: ne-resize;")).unwrap()
}

/// Tailwind `cursor-nw-resize`; css `cursor: nw-resize;`
pub fn cursor_nw_resize() -> Style {
    Style::new(css!("cursor: nw-resize;")).unwrap()
}

/// Tailwind `cursor-se-resize`; css `cursor: se-resize;`
pub fn cursor_se_resize() -> Style {
    Style::new(css!("cursor: se-resize;")).unwrap()
}

/// Tailwind `cursor-sw-resize`; css `cursor: sw-resize;`
pub fn cursor_sw_resize() -> Style {
    Style::new(css!("cursor: sw-resize;")).unwrap()
}

/// Tailwind `cursor-ew-resize`; css `cursor: ew-resize;`
pub fn cursor_ew_resize() -> Style {
    Style::new(css!("cursor: ew-resize;")).unwrap()
}

/// Tailwind `cursor-ns-resize`; css `cursor: ns-resize;`
pub fn cursor_ns_resize() -> Style {
    Style::new(css!("cursor: ns-resize;")).unwrap()
}

/// Tailwind `cursor-nesw-resize`; css `cursor: nesw-resize;`
pub fn cursor_nesw_resize() -> Style {
    Style::new(css!("cursor: nesw-resize;")).unwrap()
}

/// Tailwind `cursor-nwse-resize`; css `cursor: nwse-resize;`
pub fn cursor_nwse_resize() -> Style {
    Style::new(css!("cursor: nwse-resize;")).unwrap()
}

/// Tailwind `cursor-zoom-in`; css `cursor: zoom-in;`
pub fn cursor_zoom_in() -> Style {
    Style::new(css!("cursor: zoom-in;")).unwrap()
}

/// Tailwind `cursor-zoom-out`; css `cursor: zoom-out;`
pub fn cursor_zoom_out() -> Style {
    Style::new(css!("cursor: zoom-out;")).unwrap()
}

