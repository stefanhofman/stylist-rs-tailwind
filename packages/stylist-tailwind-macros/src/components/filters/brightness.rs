use stylist::{css, Style};

/// Tailwind `brightness-0`; css `filter: brightness(0);`
pub fn brightness_0() -> Style {
    Style::new(css!("filter: brightness(0);")).unwrap()
}

/// Tailwind `brightness-50`; css `filter: brightness(.5);`
pub fn brightness_50() -> Style {
    Style::new(css!("filter: brightness(.5);")).unwrap()
}

/// Tailwind `brightness-75`; css `filter: brightness(.75);`
pub fn brightness_75() -> Style {
    Style::new(css!("filter: brightness(.75);")).unwrap()
}

/// Tailwind `brightness-90`; css `filter: brightness(.9);`
pub fn brightness_90() -> Style {
    Style::new(css!("filter: brightness(.9);")).unwrap()
}

/// Tailwind `brightness-95`; css `filter: brightness(.95);`
pub fn brightness_95() -> Style {
    Style::new(css!("filter: brightness(.95);")).unwrap()
}

/// Tailwind `brightness-100`; css `filter: brightness(1);`
pub fn brightness_100() -> Style {
    Style::new(css!("filter: brightness(1);")).unwrap()
}

/// Tailwind `brightness-105`; css `filter: brightness(1.05);`
pub fn brightness_105() -> Style {
    Style::new(css!("filter: brightness(1.05);")).unwrap()
}

/// Tailwind `brightness-110`; css `filter: brightness(1.1);`
pub fn brightness_110() -> Style {
    Style::new(css!("filter: brightness(1.1);")).unwrap()
}

/// Tailwind `brightness-125`; css `filter: brightness(1.25);`
pub fn brightness_125() -> Style {
    Style::new(css!("filter: brightness(1.25);")).unwrap()
}

/// Tailwind `brightness-150`; css `filter: brightness(1.5);`
pub fn brightness_150() -> Style {
    Style::new(css!("filter: brightness(1.5);")).unwrap()
}

/// Tailwind `brightness-200`; css `filter: brightness(2);`
pub fn brightness_200() -> Style {
    Style::new(css!("filter: brightness(2);")).unwrap()
}

