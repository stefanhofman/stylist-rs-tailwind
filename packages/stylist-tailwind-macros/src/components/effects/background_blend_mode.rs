use stylist::{css, Style};

/// Tailwind `bg-blend-normal`; css `background-blend-mode: normal;`
pub fn bg_blend_normal() -> Style {
    Style::new(css!("background-blend-mode: normal;")).unwrap()
}

/// Tailwind `bg-blend-multiply`; css `background-blend-mode: multiply;`
pub fn bg_blend_multiply() -> Style {
    Style::new(css!("background-blend-mode: multiply;")).unwrap()
}

/// Tailwind `bg-blend-screen`; css `background-blend-mode: screen;`
pub fn bg_blend_screen() -> Style {
    Style::new(css!("background-blend-mode: screen;")).unwrap()
}

/// Tailwind `bg-blend-overlay`; css `background-blend-mode: overlay;`
pub fn bg_blend_overlay() -> Style {
    Style::new(css!("background-blend-mode: overlay;")).unwrap()
}

/// Tailwind `bg-blend-darken`; css `background-blend-mode: darken;`
pub fn bg_blend_darken() -> Style {
    Style::new(css!("background-blend-mode: darken;")).unwrap()
}

/// Tailwind `bg-blend-lighten`; css `background-blend-mode: lighten;`
pub fn bg_blend_lighten() -> Style {
    Style::new(css!("background-blend-mode: lighten;")).unwrap()
}

/// Tailwind `bg-blend-color-dodge`; css `background-blend-mode: color-dodge;`
pub fn bg_blend_color_dodge() -> Style {
    Style::new(css!("background-blend-mode: color-dodge;")).unwrap()
}

/// Tailwind `bg-blend-color-burn`; css `background-blend-mode: color-burn;`
pub fn bg_blend_color_burn() -> Style {
    Style::new(css!("background-blend-mode: color-burn;")).unwrap()
}

/// Tailwind `bg-blend-hard-light`; css `background-blend-mode: hard-light;`
pub fn bg_blend_hard_light() -> Style {
    Style::new(css!("background-blend-mode: hard-light;")).unwrap()
}

/// Tailwind `bg-blend-soft-light`; css `background-blend-mode: soft-light;`
pub fn bg_blend_soft_light() -> Style {
    Style::new(css!("background-blend-mode: soft-light;")).unwrap()
}

/// Tailwind `bg-blend-difference`; css `background-blend-mode: difference;`
pub fn bg_blend_difference() -> Style {
    Style::new(css!("background-blend-mode: difference;")).unwrap()
}

/// Tailwind `bg-blend-exclusion`; css `background-blend-mode: exclusion;`
pub fn bg_blend_exclusion() -> Style {
    Style::new(css!("background-blend-mode: exclusion;")).unwrap()
}

/// Tailwind `bg-blend-hue`; css `background-blend-mode: hue;`
pub fn bg_blend_hue() -> Style {
    Style::new(css!("background-blend-mode: hue;")).unwrap()
}

/// Tailwind `bg-blend-saturation`; css `background-blend-mode: saturation;`
pub fn bg_blend_saturation() -> Style {
    Style::new(css!("background-blend-mode: saturation;")).unwrap()
}

/// Tailwind `bg-blend-color`; css `background-blend-mode: color;`
pub fn bg_blend_color() -> Style {
    Style::new(css!("background-blend-mode: color;")).unwrap()
}

/// Tailwind `bg-blend-luminosity`; css `background-blend-mode: luminosity;`
pub fn bg_blend_luminosity() -> Style {
    Style::new(css!("background-blend-mode: luminosity;")).unwrap()
}

