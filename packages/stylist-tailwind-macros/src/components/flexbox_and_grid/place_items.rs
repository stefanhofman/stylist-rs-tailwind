use stylist::{css, Style};

/// Tailwind `place-items-start`; css `place-items: start;`
pub fn place_items_start() -> Style {
    Style::new(css!("place-items: start;")).unwrap()
}

/// Tailwind `place-items-end`; css `place-items: end;`
pub fn place_items_end() -> Style {
    Style::new(css!("place-items: end;")).unwrap()
}

/// Tailwind `place-items-center`; css `place-items: center;`
pub fn place_items_center() -> Style {
    Style::new(css!("place-items: center;")).unwrap()
}

/// Tailwind `place-items-baseline`; css `place-items: baseline;`
pub fn place_items_baseline() -> Style {
    Style::new(css!("place-items: baseline;")).unwrap()
}

/// Tailwind `place-items-stretch`; css `place-items: stretch;`
pub fn place_items_stretch() -> Style {
    Style::new(css!("place-items: stretch;")).unwrap()
}

