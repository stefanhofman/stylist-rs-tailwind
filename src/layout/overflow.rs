use stylist::{css, Style};

/// Tailwind `overflow-auto`; css `overflow: auto;`
pub fn overflow_auto() -> Style {
    Style::new(css!("overflow: auto;")).unwrap()
}

/// Tailwind `overflow-hidden`; css `overflow: hidden;`
pub fn overflow_hidden() -> Style {
    Style::new(css!("overflow: hidden;")).unwrap()
}

/// Tailwind `overflow-clip`; css `overflow: clip;`
pub fn overflow_clip() -> Style {
    Style::new(css!("overflow: clip;")).unwrap()
}

/// Tailwind `overflow-visible`; css `overflow: visible;`
pub fn overflow_visible() -> Style {
    Style::new(css!("overflow: visible;")).unwrap()
}

/// Tailwind `overflow-scroll`; css `overflow: scroll;`
pub fn overflow_scroll() -> Style {
    Style::new(css!("overflow: scroll;")).unwrap()
}

/// Tailwind `overflow-x-auto`; css `overflow-x: auto;`
pub fn overflow_x_auto() -> Style {
    Style::new(css!("overflow-x: auto;")).unwrap()
}

/// Tailwind `overflow-y-auto`; css `overflow-y: auto;`
pub fn overflow_y_auto() -> Style {
    Style::new(css!("overflow-y: auto;")).unwrap()
}

/// Tailwind `overflow-x-hidden`; css `overflow-x: hidden;`
pub fn overflow_x_hidden() -> Style {
    Style::new(css!("overflow-x: hidden;")).unwrap()
}

/// Tailwind `overflow-y-hidden`; css `overflow-y: hidden;`
pub fn overflow_y_hidden() -> Style {
    Style::new(css!("overflow-y: hidden;")).unwrap()
}

/// Tailwind `overflow-x-clip`; css `overflow-x: clip;`
pub fn overflow_x_clip() -> Style {
    Style::new(css!("overflow-x: clip;")).unwrap()
}

/// Tailwind `overflow-y-clip`; css `overflow-y: clip;`
pub fn overflow_y_clip() -> Style {
    Style::new(css!("overflow-y: clip;")).unwrap()
}

/// Tailwind `overflow-x-visible`; css `overflow-x: visible;`
pub fn overflow_x_visible() -> Style {
    Style::new(css!("overflow-x: visible;")).unwrap()
}

/// Tailwind `overflow-y-visible`; css `overflow-y: visible;`
pub fn overflow_y_visible() -> Style {
    Style::new(css!("overflow-y: visible;")).unwrap()
}

/// Tailwind `overflow-x-scroll`; css `overflow-x: scroll;`
pub fn overflow_x_scroll() -> Style {
    Style::new(css!("overflow-x: scroll;")).unwrap()
}

/// Tailwind `overflow-y-scroll`; css `overflow-y: scroll;`
pub fn overflow_y_scroll() -> Style {
    Style::new(css!("overflow-y: scroll;")).unwrap()
}

